#if 1//1si for on and 0 is for off
# define INTEGER int 
INTEGER multiply(INTEGER a, INTEGER b)
{
	int result=a*b;
	return result;
	#include "endrace.h"
	
#endif //if we type 0 then it will deactivate the entire code like it never existed there 
